apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: sentinelops
  labels:
    app: frontend
spec:
  selector:
    app: frontend
  ports:
    - port: 5173
      targetPort: 5173
      protocol: TCP
      name: http
  type: ClusterIP
