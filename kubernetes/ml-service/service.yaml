apiVersion: v1
kind: Service
metadata:
  name: ml-service
  namespace: sentinelops
  labels:
    app: ml-service
spec:
  selector:
    app: ml-service
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  type: ClusterIP
